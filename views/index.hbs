<br><br>

<div class="row d-flex justify-content-center">
    <div class="text-center">
        {{#if session}}
        <h1>Welcome user</h1>
        {{else}}
        <h1>Welcome anonymous</h1>
        {{/if}}
    </div>
</div>